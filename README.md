# ChemicalApparatus-
App for calculation scheme with chemical apparatuses by 0d models

## Решатель аппаратов (веб)

`Решатель_аппаратов.html` — самодостаточный браузерный решатель 0d-моделей
(один файл, без установки; графики — Plotly). Тот же файл лежит и внутри
`Химаппараты.zip` (`Химаппараты/Решатель_аппаратов.html`).

Режимы:

- **Один аппарат** — EOH/HRU/CHE/FHE; стационар (прямой), неявный и явный Эйлер;
  метрика попадания установившегося значения в целевой интервал.
- **Связка → Открытая цепочка** — масло проходит аппараты последовательно
  (sequential-modular).
- **Связка → Замкнутый контур масла** — масло циркулирует
  **Резервуар → Насос → Утилизатор тепла → Резервуар**; в утилизаторе масло
  греется дымовым газом, который после отдачи тепла **выбрасывается в атмосферу**.
  Это задача с рециклом: стационар ищется разрывом контура tear-потоком и
  под-релаксацией (§2.5 рекомендаций), равновесие — баланс «тепло от газа =
  потери резервуара в среду». Энергобаланс контура замыкается точно; неявный
  и явный Эйлер сходятся к одному стационару (явный при крупном Δt теряет
  устойчивость).

Подробный разбор моделей и улучшений — в
`Химаппараты/РЕКОМЕНДАЦИИ_по_улучшению_моделей.md` внутри архива.
